name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel an in-progress run when the same branch is pushed again.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      # Uncomment once the project has lint / test scripts in package.json.
      # - name: Lint
      #   run: npm run lint
      # - name: Test
      #   run: npm test
